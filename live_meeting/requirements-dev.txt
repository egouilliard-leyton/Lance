# CPU-only dev/test dependencies for the live_meeting PoC.
# Deliberately minimal and self-contained: it does NOT pull the heavy root requirements.txt
# (torch, transformers, diffusers, ...). The full suite runs on a bare CPU box / CI with just these.
numpy
imageio
imageio-ffmpeg
decord==0.6.0
Pillow
websockets>=12.0
pytest>=7.4
pytest-mock>=3.12
